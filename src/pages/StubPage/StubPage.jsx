import classes from './StubPage.module.scss';

export const StubPage = () => {
  return (
    <div className={classes.notification}>
      Извините, страницы не существует с таким URL
    </div>
  )
}